#!/bin/sh

set -e

umask 077

basedir=~/.reec
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/reec.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# REEC does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/reec/reec-qt "$@"
